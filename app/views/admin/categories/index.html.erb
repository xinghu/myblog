<h1>分类列表</h1>

<table>
  <tr>
    <th>名称</th>
    <th>描述</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |admin_category| %>
  <tr>
    <td><%= admin_category.title %></td>
    <td><%= admin_category.desc %></td>
    <td><%= link_to '查看', [:admin, admin_category] %></td>
    <td><%= link_to '编辑', edit_admin_category_path(admin_category) %></td>
    <td><%= link_to '删除', [:admin, admin_category], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '创建新分类', new_admin_category_path %>
